
<h1 class="page-title">観劇アラート</h1>

<p>公演情報</p>

<table class="table">
  <tr>
    <td>タイトル</td>
    <td><%= @stage.title %></td>
  </tr>
  <tr>
    <td>団体</td>
    <td><%= @stage.group %></td>
  </tr>
  <tr>
    <td>劇場</td>
    <td><%= @stage.theater %></td>
  </tr>
  <tr>
    <td>公演期間</td>
    <td>
      <%= @stage.startdate %>(<%= dayoftheweek(@stage.startdate) %>) ～ 
      <%= @stage.enddate %>(<%= dayoftheweek(@stage.enddate) %>)
    </td>
  </tr>
  <tr>
    <td>詳細情報</td>
    <td><%= link_to 'Corich公演情報', 'http://stage.corich.jp' + @stage.url,
                    target: '_blank',
                    rel: 'noreferrer' %></td>
  </tr>
</table>

<button type="button" class="btn btn-danger">ログインしないで通知をもらう</button>
<button type="button" class="btn btn-primary">ログインして通知をもらう</button>

<section>
  <div class="alert-on-notlogin">
    <%= form_for(@user, url: {controller: 'users', action: 'create'}) do |f| %>
    <h3 class="alert-title-notlogin">ログインしないで通知をもらう</h3>
    <p><span class="alert-step">STEP1</span>通知メール用のメールアドレスを入力してください。</p>
    <p class="alert-indent">
      <%= f.text_field :email, size: 50 %>
    </p>
    <p><br></p>
    <p><span class="alert-step">STEP2</span>通知メールを送る日にちを指定してください。</p>
    <p class="alert-indent">
      <%= check_box 'alert', 'seven_days_ago', {}, true, false %>
      <span class="alert-text">初日の7日前</span>
      <%= check_box 'alert', 'three_days_ago', {}, true, false %>
      <span class="alert-text">初日の3日前</span>
      <%= check_box 'alert', 'one_days_ago', {}, true, false %>
      <span class="alert-text">初日の1日前</span>
    </p>
    <%= f.submit "OK", class: "btn btn-primary" %>
    <% end %>
  </div>
</section>