
<h1 class="page-title">観劇アラート</h1>

<table class="table">
  <% @stages.each do |stage| %>
  <tr>
    <td class="startdate"><%= stage.startdate %>(<%= dayoftheweek(stage.startdate) %>)</td>
    <td><%= stage.title %> &nbsp; <%= stage.group %></td>
    <td><%= stage.theater %></td>
  </tr>
  <% end %>
</table>

<%= paginate @stages %>